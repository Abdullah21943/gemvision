fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# ML
# tensorflow>=2.18 required: Keras 3's .keras format keeps evolving (e.g. a
# Dense layer's saved config gained a `quantization_config` key), and a model
# trained on a newer TF/Keras (Colab, which tracks latest) fails to load on
# an older pin with "Unrecognized keyword arguments passed to Dense". Pin to
# a minimum instead of an exact version so this keeps working as Colab's
# environment moves forward.
tensorflow>=2.18
numpy>=2.0
pillow==10.4.0
xgboost==2.1.1
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3

# Training helpers
kaggle==1.6.17
matplotlib==3.9.2
